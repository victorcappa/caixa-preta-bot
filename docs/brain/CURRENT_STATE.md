# Current state

Verified against the current worktree on 2026-09-14. Code wins if this snapshot drifts.

## Runtime and architecture

- Next.js App Router 15 with React 19 and server routes under `app/api/`.
- Local runtime is fixed to `http://localhost:3000` by `scripts/require-port-3000.js`.
- `lib/showState.js` owns the in-process show state: conversation, public message, Scene Zero, games, suitcases, projection windows, cues, glitch, audio, research, Instagram status, and performance events.
- Public screens receive state through `app/api/events/route.js`; `lib/publicRealtime.js` adds revision-based reconciliation through `app/api/state/route.js`.
- Public projection windows register through `app/api/projection/route.js`. Public routes are declared in `lib/projectionScreens.js`; private controller routes are declared in `lib/controllerSurfaces.js`.
- Prompt composition lives in `prompts/`; curated play knowledge lives in `knowledge/`; the OpenAI boundary is `lib/openai.js`.

## How to start

Install with `npm install`, configure `OPENAI_API_KEY` in `.env.local` for live model calls, then run `npm run dev`. See `docs/brain/OPERATIONS.md` for show startup and validation boundaries.

## Implemented

- Main public chatbot at `/` and private technical hub at `/operator`. The projected bot line and idle cursor use Modern DOS 8×16 by default; the Operator exposes a live font preview and selection between local Modern DOS 8×16/9×16, VT323, and the original Courier, alongside typing speed and click-frequency controls. The choice is shared through `showState.robotSound`/SSE by Principal and Videomapping; operator controls retain their existing typography. Modern DOS's CC0 license is bundled with its local font files.
- Alternative `/videomapping` 2x2 presentation reuses the same `Chat`, `showState`, and realtime connection. Switching between `/` and `/videomapping` in a controlled projection window changes layout in place.
- Scene Zero controller at `/cena-0-controller` and equivalent videomapping controller at `/videomapping-controller`. Its right-side index is operational: BOOT, ESCUTA DE DECIBÉIS, ESQUENTAR PÚBLICO, ESCOLHER PARTICIPANTE, and JOGO DAS MALAS can be re-entered from the menu. Returning to ESCUTA DE DECIBÉIS keeps the completed BIOS at 100%, resets later verification progress, and immediately reopens microphone listening. Returning from the suitcases interrupts their active game, timers, browser routine, glitch, and final blackout before restoring the selected earlier stage.
- Before the required play-dead prompt, the human-verification introduction and ten authored agreement steps use the normal chatbot path. The attention signal always plays its phrase twice, including after `Quando eu fizer este som...` finishes typing and before `...você deve olhar para a tela.`. The sequence demonstrates the real five-second timer, then explains and demonstrates error and success. Only afterward the bot shows its blinking cursor, plays that same double notification, writes `...`, and comments sarcastically that the audience's alertness is inconclusive before `Podemos começar?`. Disabled `START` and `GAME OVER` choices appear publicly, while active versions remain in the operator panel; selecting them triggers distinct start and game-over sounds. Manual mode advances cursor, double notification, ellipsis, comment, and every other agreement as separate steps; automatic mode uses established delays. After the operator choice animates for 1.4 seconds, its authored comment replaces the question and releases play-dead. With no authored verification title, its progress bar stays a compact top HUD; videomapping retains its status quadrant.
- In the Scene Zero controller, `←` returns one operational stage: within BIOS it rewinds one boot step and pauses autoplay; from sound check it returns to BOOT, from warmup to sound check, from participant selection to warmup, and from suitcases to participant selection. At the first BIOS step it returns to BOOT standby. It does not replay or erase a previously published question; the established stage-return actions handle timer and game cancellation.
- During the Scene Zero question round, `↑` records the same contextual `MUITOS` response as the lateral button, and `↓` records `POUCOS`. Both shortcuts are inactive outside an active question and while typing in editable fields.
- `Space` ends the stalled BIOS or question round, draws the game once its briefing reaches `ready_for_draw`, and ends an active minigame. The first press after questions opens the authored briefing; another press when the draw button is enabled starts the draw. `→` follows the available continuation: it starts the draw, reveals it in manual mode, starts a ready minigame timer (including Tapão's second round), or resumes a paused timer. Pausing clears the obsolete timer-expiry transition. These shortcuts leave other stages and form fields alone.
- Participant selection opens in green with `Agora preciso de participantes. Levantem a mão. Vocês têm cinco segundos.` After typing, automatic starts a large centered borderless 5-second number; manual keeps the line until `→`. The name stays static for 5 seconds or until manual `→`; only then does the center-stage call type.
- The suitcase entrance omits the model duplicate, old overlays, and initial glitch. Its two authored green screens are the rules and Ricardinho's full sarcastic cue; manual waits for `→` between them and before drawing. Later draws use distinct short sarcastic Ricardinho lines plus their emergence glitches. Message IDs guard stale acknowledgements.
- Manual pauses twice after each suitcase draw: `→` shows `ABRA A MALA`, then another starts its content. Automatic retains timed reveal/start. `→` starts a prepared Mala 2 timer but cannot leave an active or failed/retrying challenge; only a concluded game releases the next suitcase.
- Scene Zero boot/BIOS, sound check measured in the controller, human-verification warmup, 180 authored prompts (20 in each of the first three intensities and 60 in each of `PROVOCATIVE` and `SOCIAL PRESSURE`), manual override, one selected warmup mini game, participant selection, and suitcase flow. The right arrow is the primary forward control: from standby it starts `BOOT`, while booting it advances one BIOS step, at the intentional 78% stall it executes `ENCERRAR BIOS`, after the BIOS it consumes the current pending transition, and during the question round it runs the reaction-plus-draw flow with intensity `4 · PROVOCATIVE` by default. In manual mode, every public line must finish typing before its pending continuation unlocks, and each `→` consumes exactly one transition: `START`/`GAME OVER`, its sarcastic comment, the required timer, its acknowledgement, reactions, and later question steps do not chain automatically. Automatic mode keeps the typewriter/read delay and then continues. `ENCERRAR BIOS` remains visible as a click fallback. The play-dead timer starts only after its line; finishing it publishes `OBEDIENTES... ÓTIMO.`, which remains a separate manual step before the first randomly selected intensity-4 provocation. For every later question, the operator records a contextual `MUITOS` or `POUCOS` result; the backend requires that response, publishes a non-repeated green reaction, and only then releases the next draw. Minigames start directly on their real timer: Tapão runs two 10-second rounds; Piscada and Serinho each run one 20-second round. The shared fixed timer projects only its number.
- Warmup draws remember every prompt ID used in the current round and avoid reusing one at the selected intensity until its available prompts are exhausted. Each draw chooses among several diverse candidates. Sound-check waiting comments are shuffled for each listening pass, with the opening greeting fixed and linked exchanges kept in sequence.
- Current suitcase order is Mala 2 -> pre-draw hangman -> Mala 3 -> Mala 1. The hangman must be won before the second suitcase draw: throughout the challenge, `currentSuitcase` remains 2 and `openedSuitcases` remains `[2]`; its completed green result releases the normal Mala 3 announcement and roulette. Manual mode exposes that release from both `challenge_result` and `challenge_complete`, so a delayed/missed typewriter acknowledgement cannot strand the operator. Draws last five seconds and end in `ABRA A MALA`; challenge panels appear only while active. Mala 2 gives exactly 15 seconds after `VALENDO!`; no time extension exists, and only `TOCAR UBA UBA HEY` starts its music. Failure shows a red sarcastic line about everyone waiting/substance/attention and blocks the next draw. Automatic mode restarts after the line; manual mode enters retry `ready` and waits for `→`. Hangman starts with 60 seconds; after a loss, the same word reopens with 30 additional seconds, without revealing the answer, and manual mode waits for its own retry advance. Hangman errors add impact, shake, and progressive red interference without textual flight states. Emergence glitches between suitcases also stop on an explicit manual gate before the next sarcastic line. Before Mala 1, the bot jokes about drawing from the single remaining option, then still runs the roulette. The final suitcase keeps its turntable, BIOS/glitch, and blackout sequence.
- Manual suitcase recovery exposes one draw button for each suitcase. It cancels the current suitcase activity, timers, audio, hangman, Instagram, BIOS/glitch, and pending callbacks before entering the selected suitcase through the normal public roulette, while preserving the opened-suitcase history. A transition-token lock accepts only one right-arrow advance until the resulting SSE snapshot changes the current stage, preventing rapid double taps from consuming two manual gates.
- Authored suitcase instructions are inserted without replacing the existing mechanics: wait/balloons/key/15 seconds before the first challenge timer, hangman before the second suitcase can be opened, record pickup between the second and last suitcases, and turntable use before the final tutorial-ending sequence. Each Mala 2 instruction waits for its own typewriter acknowledgement; manual mode requires `→` between the two lines, while automatic mode holds the completed line for two seconds before continuing. The final `Use o disco no toca-discos.` remains static after typing and requires the next operator arrow/button before the game finishes and the unlock sequence begins. The fixed right-hand Scene Zero rail also has two `INSTRUÇÃO AVULSA` inputs at every stage: `PARA O BOT` makes the Caixa Preta speak in first person about its own state or idea, while `PARA O PARTICIPANTE / PÚBLICO` addresses the person or plateia. Both publish one short in-character line through normal chat even while an automatic instruction is active and never change stage, timers, score, current suitcase, or automatic progression.
- The controller header intentionally exposes only Principal, Videomapping, Sound Control, Operator, Glitch, and Treino. `/sound-control` owns the shared procedural sound controls and the Scene Zero microphone-sensitivity adjustment without navigating the public projection. The Scene Zero controller is also an audio relay sink and its fixed lateral index has a `CHAMAR ATENÇÃO` cue, so that operating path does not depend on keeping Sound Control open. Other controller routes remain registered and reachable from their direct operating paths; the former global blackout tab bar is not rendered.
- Scene 1 Queda Aviao public/controller pair with live text transport, sampler, per-sample effects, global volume, and blackout.
- Unified Scene 2A Forca G public/controller pair for media, audio voices, G-LOC, text, shaders, presets, and stop controls. Legacy shader routes redirect to it.
- Baralho Morbido public/controller pair with its own bounded media/timeline state.
- Generic editable cue controllers and public cue stages for Transicao Psicodelica, Tea For Two, Piloto Videogame, and Tecnologia x Floresta.
- Global glitch, display blackout, robot sound with shared pitch/typing controls and `ROBÔ ATUAL` / default `WINDOWS 95 / 8-BIT` PC-speaker style (including Scene Zero BIOS, draw, minigame, and hangman cues), scene-specific private notes, training UI, game director, performance overlays, and manual Instagram browser controls. Public typewriter timing defaults to 60 ms per character and remains operator-adjustable.
- In Principal, `INICIAR REELS + COMENTÁRIOS` sits below the audience reaction controls. It opens the shared embedded Reels panel and starts the first chat/comment round promptly; it generates distinct public chat lines and Reel comments in parallel while navigating Reels. For each visible Reel, including one without an exposed permalink or video source, it opens the Reel comments and attempts a separate GPT comment from the authenticated Instagram account, spaced by at least 20 seconds. The routine chooses a visible, enabled publish control near the comment field; its last send status and failure reason remain in Instagram state. `PARAR REELS / COMENTÁRIOS`, `STOP ALL`, and timer pause/cancel/end or expiry close the Reels browser and embedded panel. An unconfirmed send is not retried on the same Reel key. Login remains manual.
- The pre-second-suitcase hangman draws a public theme with the same roulette language as the warmup minigame before choosing a word from that theme. The public Hangman panel displays the result prominently as `TEMA: ...`, while keeping the word secret until the usual reveal.
- Public and private concerns are separate: controller notes and controls are not projected or sent to the model.

## Partially implemented or content-dependent

- Transicao Psicodelica, Tea For Two, and Piloto Videogame public routes intentionally start black and show/play only configured cues. Their controller mechanics exist; completeness depends on authored cues and media.
- Tecnologia x Floresta has a dedicated dark public display and editable audio controller, but the code describes it as a layer rather than a finished visual scene.
- Several editable cue defaults have empty asset paths. The controls exist, but an empty pad is not a completed show cue.
- Live OpenAI research, model responses, browser Instagram interaction, microphone behavior, autoplay, and physical audio depend on local credentials, browser permissions, current session state, and hardware. Static checks do not prove them.

## Disabled by default or constrained

- Autonomous Instagram navigation is hard-disabled in `lib/research/ResearchDirector.js`; external navigation requires explicit operator action and is guarded by `lib/externalNavigationGuard.js`.
- Performative research is off unless `CAIXA_PRETA_PERFORMATIVE_RESEARCH` is explicitly true.
- Old `/forca-g-shaders` routes are compatibility redirects, not a separate active scene system.
- No versioned CI or deployment workflow is present. There is no repository-backed production deployment procedure to claim as implemented.

## Known operational constraints

- State is process-local. Restarting Next creates a new show session; `/reset` clears the current show session.
- The global singleton is version-gated by `STORE_VERSION` in `lib/showState.js`; stale dev processes can retain old state/code combinations.
- Browser media and microphone paths require real browser interaction and permissions; physical output needs a separate sound check.
- Browser checks require the expected local server and deliberate waits for SSE/timer convergence.
- The repository contains archived `bot-old/`; it is not the current implementation.

## Active worktree context

The current branch is `feature-verificacao-humana-bios`. At this snapshot, Scene Zero unlock/BIOS, warmup, public projection, and videomapping files have uncommitted changes. Inspect `git status` and the relevant diff before editing those domains; do not overwrite unrelated work. Treat this paragraph as transient and replace it when that work lands or is abandoned.
