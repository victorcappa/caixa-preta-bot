import SceneProjectionPage from "@/components/SceneProjectionPage";

export const metadata = {
  title: "Transicao Psicodelica",
  description: "Projecao publica preta para a transicao psicodelica"
};

export default function TransicaoPsicodelicaPage() {
  return <SceneProjectionPage controllerId="transicao-psicodelica" />;
}
